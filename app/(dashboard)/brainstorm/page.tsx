import { TabPlaceholder } from "@/components/shared/tab-placeholder";

export default function BrainstormPage() {
  return (
    <TabPlaceholder
      title="Brainstorm"
      description="Chat with any LLM via OpenRouter — task-based auto-routing, skill file injection, and a searchable personal prompt library."
    />
  );
}
