import { TabPlaceholder } from "@/components/shared/tab-placeholder";

export default function CharactersPage() {
  return (
    <TabPlaceholder
      title="Characters"
      description="Soul ID management — train a character once from 5-20 reference photos via Higgsfield, then reuse it in every generation across the studio."
    />
  );
}
