import { TabPlaceholder } from "@/components/shared/tab-placeholder";

export default function AudioPage() {
  return (
    <TabPlaceholder
      title="Audio Assets"
      description="Generate music, SFX, and voiceover with FAL stable-audio, keep a searchable library, and attach tracks straight to the Timeline."
    />
  );
}
