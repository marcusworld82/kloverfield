import { TabPlaceholder } from "@/components/shared/tab-placeholder";

export default function StoryboardPage() {
  return (
    <TabPlaceholder
      title="Storyboard"
      description="Type a story concept, get scene-by-scene cards with generated frames, then push clips straight to the Timeline."
    />
  );
}
