import { TabPlaceholder } from "@/components/shared/tab-placeholder";

export default function CanvasPage() {
  return (
    <TabPlaceholder
      title="Canvas"
      description="Infinite node-based workflow canvas (Magnific Spaces style) — chain text, image, video, character, and audio nodes into repeatable pipelines."
    />
  );
}
