import { TabPlaceholder } from "@/components/shared/tab-placeholder";

export default function TimelinePage() {
  return (
    <TabPlaceholder
      title="Timeline"
      description="Multi-track video editor powered by Remotion — drag, trim, and layer video, audio, and captions, then export MP4 server-side."
    />
  );
}
