import { TabPlaceholder } from "@/components/shared/tab-placeholder";

export default function SettingsPage() {
  return (
    <TabPlaceholder
      title="Settings"
      description="API key management, account profile, theme, and a per-provider usage and cost dashboard with daily spend ceilings."
    />
  );
}
